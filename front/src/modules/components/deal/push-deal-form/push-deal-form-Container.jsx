import { connect } from "react-redux"
import DealPush from "./push-deal-form"


const mapStateToProps = (state) => {

    return {


    }
}
const mapDispatchToProps = (dispatch) => {

    return {


    }
}


export const DealPushContainer = connect(mapStateToProps, mapDispatchToProps)(DealPush)